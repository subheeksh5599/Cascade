[project]
name = "cascade-contracts"
description = "Cascade — Recursive Money Flow Graph Registry on Stacks"
clarity_version = 3
epoch = 3.0

[repl]
costs_version = 3
analysis = ["check_checker"]

[contracts.cascade-registry]
path = "contracts/cascade-registry.clar"
clarity_version = 3
epoch = 3.0
